/node_modules/
/dist-commonjs

# dist/ + dist-core/ ARE checked in. Consumers (ordpool/frontend,
# cat21-indexer/frontend, cat21-wallet, cat21.space) pin a SDK git
# SHA and get pre-built artifacts from the tarball — no postinstall
# build, no ng-packagr-in-node_modules failure mode. Rebuild +
# commit BOTH alongside any source change. See the SDK CLAUDE.md
# "Shipped artifacts" section for the workflow.

# Playwright + Xverse: fetched fresh in CI, never committed
/e2e/extensions/
/e2e/playwright/fixtures/sdk-harness.js
/playwright-report/
/test-results/

# System Files
.DS_Store
Thumbs.db

# IDE files
.vscode/settings.json
