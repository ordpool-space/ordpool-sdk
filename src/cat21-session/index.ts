/**
 * `ordpool-sdk/cat21-session` — the session-message shape.
 *
 * A subpath so a consumer that only builds or checks a session message does
 * not pull the package barrel.
 */
export * from './session-message.js';
