/**
 * `ordpool-sdk/family` — the shared cross-surface family conventions.
 *
 * A subpath so a consumer rendering the family footer or the funding-coin
 * safety copy does not pull the package barrel.
 */
export * from './ordpool-family.js';
export * from './coin-check-promise.js';
